# Implementation Plans

Generated by the improve skill on 2026-08-28 (advisor session, commit
`17ab79a`), focused on the money/monetization pipelines per the operator's
invocation. The operator was not available at selection time, so per the
skill's non-interactive default these are the **top findings by leverage**
(impact ÷ effort, discounted by risk); everything else vetted in the audit is
recorded below so it is not lost or re-audited.

Context: an 18-pipeline adversarial money audit ran on 2026-08-27/28 (133
agents, every finding refutation-checked; 114 candidates → 74 confirmed → 46
distinct). Thirteen distinct defects were fixed and deployed the same day —
these plans are the highest-leverage items that REMAIN. Each plan was written
against code the advisor re-read personally at `17ab79a`.

Executors: read your plan fully before starting, honor its STOP conditions,
run every verification gate, and update your row here. **Never `git push` in
this repo — pushing `main` deploys production via GitHub Actions.** Leave
pushing to the operator.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Recovery emails dedupe per customer (unblocks 8 warm leads) | P1 | S | — | DONE (2026-08-28, by operator instruction) |
| 002 | Refund/reject actually takes paid content down | P1 | S | — | DONE (2026-08-28) |
| 003 | Sales Desk bills multi-location listings per location | P1 | M | — | DONE (2026-08-28) |
| 004 | Payout cycle pages by eligibility; nothing silently skipped | P1 | M | index deployed & READY before code | DONE (2026-08-28) |
| 005 | Finance numbers tell the truth | P1 | M | — | DONE (2026-08-28) |
| 006 | Hygiene: dead payout setting, claim rate limit, CRON_SECRET runbook | P2 | S | — | DONE (A+B 2026-08-28; C executed by operator authority same day) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (rationale)

## Dependency notes

- The six plans are mutually independent and may be executed in any order;
  the numbering is the recommended order by leverage.
- 004 carries an **operator step**: the `transfers (status, eligible_at)`
  Firestore index should be deployed (`firebase deploy --only
  firestore:indexes`) and READY before the code ships. The plan includes a
  runtime fallback so wrong ordering degrades loudly, not fatally.
- 001 makes recovery emails SAFE; actually enabling them
  (`CHECKOUT_RECOVERY_EMAILS=on` + a scheduler job) remains an operator
  decision, deliberately outside every plan.
- 006 Part C (secret rotation) is operator-only; no executor deliverable.

## Update 2026-08-28 — operator ordered full implementation

The operator instructed "fix all findings, implement all suggestions", ending
the advisory hold. All six plans above were executed the same day, plus the
following parked findings (statuses moved here from the NOT-planned list):
upgrade path (`/api/directory/change-plan`, in-place proration; dashboard
boost buttons now change plans instead of opening second checkouts), MFA on
staff money APIs, Sales-Desk billing attribution (upsertSubscription no longer
null-wipes; claim approval stamps the owner onto the subscription), buyer
purchase confirmations + moderation outcome emails (bilingual), the
Sponsored-Story/Custom fulfillment queue (`/admin/fulfillment`), and the
newsletter slot occupancy guard. Still open: the webhook/payout DI test
harness (L), reconcile-window pinning auto-retirement (operator decision),
rep notifications, self-serve Sponsored purchase path, customer order-status
page.

## Vetted findings deliberately NOT planned this round

Confirmed in the audit, real, and parked — so nobody re-audits them. Ordered
roughly by value; any can be promoted to a plan on request.

- **No plan-change/upgrade path** (HIGH value, M effort): the double-billing
  guard now 409s self-serve upgrades; a real `stripe.subscriptions.update`
  proration flow — plus stopping an upgrade payment from demoting an approved
  owner to `pending_approval` — is product work needing operator input on
  proration policy.
- **2FA on money APIs** (M): MFA is enforced by the two admin layouts, not by
  the API guards; checkout-minting/approval/payout endpoints accept
  password-only sessions. Needs a decision on which server guard owns the
  check (`getServerUserProfile` consumers) and a lockout-recovery story.
- **Sales-Desk customers are billing-blind** (M): `advertiser_id` never set on
  sales-path subscriptions so `/billing` shows nothing; the customer portal
  can miss a second Stripe customer. Fix belongs with the upgrade-path work.
- **Buyer confirmations** (M/L): the webhook sends buyers nothing for any
  product; Stripe's own emails cover subscription invoices only. Notification
  infra (Novu + first-party inbox) exists and is wired to everything except
  purchases.
- **Sponsored Story / Custom Quote admin queue** (M): briefs land in
  collections with no reader; an ops email now fires per submission
  (stop-gap), but a real queue matching `/admin/jobs` is owed.
- **Newsletter slot occupancy/rotation** (S/M): approving a second sponsorship
  silently buries the first (`getNewsletterSponsor` is `limit(1)`, no
  ordering). Operational guardrail in force: approve ONE at a time.
- **Webhook/payout test harness** (L): all impure money orchestration is
  untested — the session's three self-inflicted regressions all lived there.
  Needs a DI seam + fakes; prerequisite for aggressive future webhook work.
- **Reconcile-window pinning** (S/M): chronically unpayable transfer rows can
  pin the nightly reconcile's scan window; deferred pending an operator
  decision on auto-retiring owed money (004 adds the cycle-side visibility).
- **Founding cap timing** (S): cap is checked at link creation, not at
  payment-landed; small overshoot window bounded by unpaid links. Largely
  mitigated by the double-count fix (`countFoundingSpotsTaken`).
- **Direction options** (operator's call, from the audit's direction pass):
  self-serve purchase path for the $99/mo Sponsored plan (grid + plan +
  `MyListingsBoost` surface all exist); a customer order-status page (token
  infra exists); rep notifications through the existing Novu wiring.

## Findings considered and rejected

- *Split-override delete no-op* — real (Firestore merge preserves deleted
  keys) but godmode-only, visible in the UI when it happens, low stakes:
  not worth a plan.
- *Commission race sub-findings beyond what shipped* — the mid-flight
  clawback/pay race is already guarded transactionally (`recordPaid` refuses
  to demote reversed shares; pre-transfer re-read); residual sub-second
  windows require a twice-monthly cron colliding with a refund at the exact
  instant and Stripe itself refuses transfers against refunded charges.
  Defense in depth already adequate.
- *ACH/delayed-payment handling* — ACH is not enabled on the account; revisit
  only if it ever is.
- *Billing-page cosmetic issues* (everything "Pending", cancelled subs under
  "Active") — display-only; fold into the billing-blindness work when that
  runs.
