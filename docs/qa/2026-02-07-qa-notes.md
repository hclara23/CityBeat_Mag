# QA Notes (2026-02-07)

## Scope
- Reader app (apps/web)
- Advertiser app (apps/ads)
- Worker service (services/worker)

## Definition of Done
- `npm run lint` passes at repo root
- `npm run type-check` passes at repo root
- Ads API endpoints require authenticated advertiser in production
- Stripe checkout session can be created for a campaign
- Campaign list and detail pages render without runtime errors
- Briefs list and detail pages render without runtime errors

## Smoke Tests
1. `apps/web` landing renders and locale switch works.
2. `apps/web` briefs list loads and category filter works.
3. `apps/web` brief detail loads for a published brief.
4. `apps/web` signup -> login -> dashboard loads.
5. `apps/ads` landing renders.
6. `apps/ads` create campaign (newsletter/sponsored/banner) -> checkout returns URL.
7. `apps/ads` campaigns list loads for authenticated advertiser.
8. `apps/ads` campaign detail loads and pause/resume returns 200.
9. `apps/ads` orders list loads.

## Basic E2E Plan
- Use a test advertiser account with `is_advertiser = true`.
- Create one campaign per ad type.
- Validate Stripe session creation and redirect URL.
- Confirm campaign status transitions (pending -> active, pause/resume).
- Confirm analytics counters remain 0 when no impressions/clicks exist.

## Release Checklist
- Confirm required env vars are present for all apps.
- Apply DB migration for `ad_purchases` RLS policy update.
- Verify Stripe keys and webhook secret are configured.
- Run smoke tests above in staging.
- Validate Cloudflare/worker endpoints are reachable.

## Notes
- Automated tests not executed in this pass.
